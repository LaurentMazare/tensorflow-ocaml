(jbuild_version 1)

(executables
 ((names (mnist_conv_fnn mnist_linear_fnn mnist_multi_fnn))
  (libraries (tensorflow tensorflow.fnn))))

; (executables
;  ((names (vgg19))
;   (libraries (camlimages.jpeg camlimages.png tensorflow tensorflow.fnn))))
